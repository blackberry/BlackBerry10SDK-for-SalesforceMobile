APP_NAME = SalesforceEmptyApp

CONFIG += qt warn_on cascades10

include(config.pri)

LIBS += -lbb
LIBS += -lbbdata
LIBS += -lbbdevice
LIBS += -lbbplatform
LIBS += -lhuapi 
LIBS += -lbbsystem
LIBS += -lscreen
